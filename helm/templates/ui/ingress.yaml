{{- if .Values.ui.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "echophase.fullname" . }}-ui
  labels:
    {{- include "echophase.labels" . | nindent 4 }}
spec:
  rules:
  - host: {{ .Values.ui.ingress.host }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ include "echophase.fullname" . }}-ui
            port:
              number: {{ .Values.ui.service.port }}
{{- end }}
