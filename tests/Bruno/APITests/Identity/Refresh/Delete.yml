info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/auth/refresh"
  headers:
    - name: X-CSRF-TOKEN
      value: "{{X-CSRF-TOKEN}}"
  body:
    type: json
    data: |-
      {
        "RefreshId": "{{RefreshId}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
