info:
  name: Sessions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/sessions"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
