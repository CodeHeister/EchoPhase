info:
  name: New
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/refresh/new"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
