info:
  name: DeleteAll
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/auth/refresh/all"
  headers:
    - name: X-CSRF-TOKEN
      value: "{{X-CSRF-TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
