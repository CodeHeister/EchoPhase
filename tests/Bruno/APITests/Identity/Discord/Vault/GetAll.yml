info:
  name: GetAll
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/discord/vault"
  headers:
    - name: X-CSRF-TOKEN
      value: "{{X-CSRF-TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
