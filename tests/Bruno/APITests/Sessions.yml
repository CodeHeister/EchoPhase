info:
  name: Sessions
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/auth/refresh/sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
