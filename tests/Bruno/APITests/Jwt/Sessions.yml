info:
  name: Sessions
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/sessions"
  headers:
    - name: Authorization
      value: Bearer {{AccessToken}}
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
