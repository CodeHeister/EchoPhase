services:
  echophase:
    image: localhost:5000/echophase:latest
    restart: unless-stopped
    env_file:
      - ./.env
    ports:
      - "8080:8080"
    volumes:
      - ./EchoPhase/Logs:/app/Logs
    network_mode: host
